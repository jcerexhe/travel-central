<%= simple_form_for(@album_image) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :file %>
    <%= f.association :album %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
